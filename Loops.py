if __name__ == '__main__':          # This block ensures the code runs only when executed directly, not when imported.
    n = int(input())                # Takes input from the user, converts it to an integer, and stores it in 'n'.

for i in range(n):                  # Loops through all integers from 0 up to (n-1).
    print(i**2)                     # Prints the square of the current number 'i'.
